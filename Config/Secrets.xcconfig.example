// Copy this file to Secrets.xcconfig and paste your Zyfy API key.
// Secrets.xcconfig is gitignored and must not be committed.
//
// Phase 1 only: the key is embedded in the iOS app and will move server-side later.
// A key shipped inside an iOS binary cannot be genuinely secured.

ZYFY_API_KEY = ea_live_your_key_here
